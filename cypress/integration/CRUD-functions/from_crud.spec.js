/* create a new contact */

describe("create new Contact", function () {
  /* before each test go to the home page */
  beforeEach(() => {
    cy.visit("localhost:8000")
  })

  it.todo("create new contact")
  it.todo("add a email and save ")
  it.todo(
    "delete all emails add new ones and change the first and last name and save"
  )
  it.todo("select a contact, cancel it, and select another one")
  it.todo("delete a contact")
})
